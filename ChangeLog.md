# 更新日志 (Changelog)

所有值得注意的修改都将记录在此。  
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [v2.4.13]
### 修复
- 修复菜单样式左上角"点单设备：设备："双前缀重复问题：
  - `getDeviceInfoString` 增加 `prefix` 入参，支持调用方自定义前缀。
  - 墨痕模式默认传入「设备：」，菜单模式传入「点单设备：」。
  - 消除调用点二次拼接，菜单样式设备行现正确显示为「点单设备：Kobo Libra 2  …」。

---

## [v2.4.12]
### 新增
- **诗词板块再次扩容**：追加 `诗词第三版.txt`（100 → 200 条）。
  - 第三版 104 条剔除 3 条与第二版完全重复项，再剔除 1 条与既有同诗句重复句（苏轼《定风波》），净增 100 条。
  - 总量精确达到 200 条，保留李贺《金铜仙人辞汉歌》独立短句「天若有情天亦老」。

---

## [v2.4.11]
### 新增
- **内容全面扩容**：
  - 诗词板块由 40 条扩展至 **100 条**（内嵌 `诗词第二版.txt`，新增 `parsePoemText()` 运行时解析）。
  - 名言板块由 40 条扩展至 **200 条**（中外各 100 条，内嵌 `名言第二版.txt`）。
  - 菜谱板块由 60 道扩展至 **250 道**（主菜 100 道 / 汤菜 50 道 / 饮品 50 道 / 甜点 50 道）。
### 修改
- 菜单样式右上角标题由 `"菜单"` 改为 `"菜单：5份"`（与"总计光临本店"同行右对齐）。
- 诗词解析兼容无朝代条目（毛泽东、鲁迅等）、括号式作品名（秋瑾（绝命诗））以及卷次标注（陈毅《梅岭三章》（其一））。

---

## [v2.4.10]
### 修改
- 诗词板块微调：陈毅《梅岭三章（其一）》改为“此去泉台招旧部，旌旗十万斩阎罗。”
- 陈毅《梅岭三章（其三）》改为“取义成仁今日事，人间遍种自由花。”

---

## [v2.4.9]
### 修改
- 墨痕“时长”与菜单“总计光临本店”两处左上角栏位的时间统一换算为以“天”为单位显示（保留 1 位小数）。
- 极短时长（<0.05 天）显示“不足1天”，避免“0天 / 0.0天”的歧义。

---

## [v2.4.8]
### 修改
- 右下名言板块字号与左下诗词板块逐行完全一致：
  - 名言行 = 诗句行（`quote_size`）
  - 名人行 = 朝代·作者行（`meta_size`）
  - 出处行 = 作品名行（`meta_size`）
- 布局测量同步改用实际字号，第一行行距取诗句行 / 名言行中的较高者，防止长名言行换行后压到名人行。

---

## [v2.4.7]
### 修改
- 右下角名言出处行统一加书名号（《出处》），与左侧诗句作品名行排版一致。

---

## [v2.4.6]
### 修改
- 菜单样式特色 / 食材行不再按固定 18 / 26 字符截断，改为 `truncateToWidth` 按像素宽度截断，充分利用表格右侧空间。
- 以 `title_w` 为硬边界，保证不与“数量”栏重叠。
- 右下角名言第一行按标准引用格式显示（中文引号 + 句号“名言。”）。

---

## [v2.4.5]
### 新增
- **新增第三种样式「菜单样式」（留台单）**：
  - UI 布局与墨痕完全一致，表格改为 5 道菜（No.1/2 主菜 + No.3 汤菜 + No.4 饮品 + No.5 甜点）。
  - 菜谱全文内嵌（无外部依赖），解析时用字面 `find/sub` 截取，规避 Lua 模式对 UTF-8 的多字节陷阱。
  - 三行分别显示：菜名 / 特色 / 食材；单价列显示价格并带“元”。
  - 合计 = 5 道菜价格总和，显示于右下角合计行。
  - 所有文本替换：下单时间（仅当天日期）→ 点单设备 → 总计光临本店 → 用餐时间 → 菜单 / 留台单 / Order Slip。
- **轮流模式泛化为 N 样式顺序循环**（film → inkstain → menu），首次调用自动回落首样式。
- 右下角版权署名替换为名人名言三行（名言 / 名人 / 出处），右对齐并与左侧诗句三行水平对齐。

---

## [v2.4.4]
### 修改
- 墨痕底部布局改为**表格锚定**：
  - 表格区恒按 5 本预留高度（无书 / 少书时留白，分隔线位置稳定）。
  - 分隔线紧贴书单底部，仅留小缝隙。
  - 底部区块整体上移，自顶向下紧凑排列，诗句块不再贴底，屏幕底部自然留白。

---

## [v2.4.3]
### 修复
- 修复伪二维码成片同色问题：
  - v2.4.2 的坐标线性哈希导致相邻格高度相关，产生一半黑一半白的大色块。
  - 改为顺序推进的 Park-Miller 伪随机序列逐格填充，输出天然迷宫状散点。
- 新增共享生成器 `qrDataBitsForGrid`，供绘制与测试复用。

---

## [v2.4.2]
### 修改
- 伪二维码内部迷宫化：以确定性哈希黑白散点取代线性取模斜条纹。
- 补充真实二维码时序图案（第 6 行 / 第 6 列交替条）。
- 书单序号由 `NO.` 改为 `No.`。

---

## [v2.4.1]
### 优化
- 针对 6 寸小屏设备优化：
  - 底部边距独立压缩（作品名贴近底边框）。
  - 底部区块间距与汇总行间距收紧。
  - 表格行高按内容瘦身，墨痕书单在小屏上稳定显示 5 本。

---

## [v2.4.0]
### 重构
- 底部区域响应式重构：
  - 二维码 / 条码行与折线图日期标签行强制分离（先测量后分配，删除事后兜底）。
  - 图表高度与间距全部随 `scale` 缩放，大 / 标准 / 小屏设备空间占比自适应。

---

## [v2.3.1]
### 修改
- 版权署名回归右下角，与左下角诗句同一行（同一高度），位于朝代·作者与作品名行之上。

---

## [v2.3.0]
### 修复
- 修复墨痕诗句不轮换的问题，改为每次展示顺序推进一首（状态持久化）。
### 修改
- 尺寸单位“吋”改为“寸”。

---

## [v2.2.0]
### 修改
- 墨痕页脚诗句库替换为 40 句经典诗词。
- 单行显示改为三行显示（诗句 / 朝代·作者 / 作品名）。

---

## [v2.1.0]
### 新增
- 新增“轮流出现”模式（与“随机”并列），手势调出与锁屏共享轮换序列。

---

## [v2.0.2]
### 修改
- 墨痕样式去除“来源”栏。
- 设备行显示真实设备型号 / 屏幕尺寸 / 分辨率 / DPI。

---

## [v2.0.1]
### 修复
- 修复墨痕书单仅显示 4 本的问题（移除 1 分钟阅读门槛）。
- 书名列智能匹配屏幕宽度，书名可显示更长。

---

## [v2.0.0]
### 重大更新
- 整合墨痕壁纸引擎，移除 6 种旧卡片样式。
- 保留胶片票根，两种样式同池 50% 随机出现。

---

## [v1.x]
### 初始版本
- 原阅读小票（胶片票根 + 6 种卡片样式 + 小花按钮）。
